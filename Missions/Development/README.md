# Development missions

This folder is `nationsatwar\Missions\Development`. Put your DCS mission files (`.miz`) here.

In the Mission Editor, set the mission to run `Scripts/Init.lua` (or your script path) so it loads MOOSE from `Scripts/MOOSE/`.
